events {}
http {
    server {
        listen 80;
        location / {
            proxy_pass http://web:5000;
            proxy_connect_timeout 2s;
            proxy_read_timeout 2s;
        }
    }
}
